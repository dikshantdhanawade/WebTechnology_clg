SQl = structure query langauge use to communicate between realtional   database mysql,postegsql
-helps to add,delete,upate
-storeprocedure, 

Activities :
1.create table insert data select data upate and delete
=> 
   1. create table Student(varchar name,int id);
   2. select name,id from Student;
   3. insert into Student("Digvijay",20);
   4. update Student set name="vijay" where id=1;
   5. delete student where id=1;

2.imp for cmp
  1.websocket :- allows us real time communicton HTTP => requst or response => continouly connection 
  real life example = chatapp-whatapp,livestokprice updates,online gaming, notification 
  if we install websocket command := npm install ws;

  2. ORM instead of writing sql queries we use javascript objects 
  with orm student.findall (js object )

  Activities :
  1. deep study web socket 
  2. understand websocket with example
